import { moveEntity, changeSelection, addAttribute, addMethod, updateEntity,
ADD_ENTITY, MOVE_ENTITY, CHANGE_SELECTION, ADD_ATTRIBUTE, ADD_METHOD, UPDATE_ENTITY,
} from '../../model/actions';

test('1', () => {
});
